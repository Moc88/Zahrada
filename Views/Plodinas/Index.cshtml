@model IEnumerable<OsevniPlan.Models.Plodina>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Odruda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vyrobce)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oseto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Odruda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vyrobce)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oseto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
